# Stampia Fase 2 — Rediseño UI

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rediseñar toda la interfaz de Stampia con un look Clean Canvas (blanco editorial), tipografía Fraunces serif para headings + Plus Jakarta Sans para cuerpo, y animaciones expresivas (parallax, fade-up escalonado, scale-reveal).

**Architecture:** La landing page se implementa primero como referencia de design system. El sistema de animaciones usa `IntersectionObserver` + CSS custom properties para stagger. El parallax del hero usa `requestAnimationFrame`. Los design tokens se definen en `globals.css` con variables CSS (`--bg`, `--surface`, `--border`, etc.). Las demás páginas siguen los mismos tokens y patrones.

**Tech Stack:** Next.js 16.2.7 App Router, Tailwind v4, CSS variables, IntersectionObserver API, requestAnimationFrame, Google Fonts (Fraunces + Plus Jakarta Sans)

**Prerequisito:** Fase 1 (arquitectura) debe estar completa antes de comenzar.

---

## File Map

| Acción | Archivo |
|--------|---------|
| Modificar | `app/layout.tsx` |
| Modificar | `app/globals.css` |
| Modificar | `app/(shop)/layout.tsx` |
| Modificar | `app/(shop)/page.tsx` |
| Modificar | `app/(shop)/catalog/page.tsx` |
| Modificar | `app/(shop)/product/[slug]/page.tsx` |
| Modificar | `app/(shop)/cart/page.tsx` |
| Modificar | `app/(shop)/checkout/page.tsx` |
| Modificar | `app/(auth)/layout.tsx` |
| Modificar | `app/(auth)/login/page.tsx` |
| Modificar | `app/(auth)/register/page.tsx` |
| Modificar | `app/(account)/orders/page.tsx` |
| Modificar | `app/(account)/orders/[orderId]/page.tsx` |
| Modificar | `components/product/ProductCard.tsx` |
| Modificar | `components/product/ProductDetail.tsx` |
| Modificar | `components/customizer/CustomizerClient.tsx` |
| Modificar | `components/ui/NavUserMenu.tsx` |
| Modificar | `components/ui/StatusBadge.tsx` |
| Crear | `components/ui/AnimateOnScroll.tsx` |

---

### Task 1: Design system — globals.css y layout.tsx

**Files:**
- Modify: `app/globals.css`
- Modify: `app/layout.tsx`

- [ ] **Step 1: Agregar Fraunces a layout.tsx**

```typescript
// app/layout.tsx
import { Plus_Jakarta_Sans, Fraunces } from 'next/font/google'

const jakarta = Plus_Jakarta_Sans({
  subsets: ['latin'],
  variable: '--font-sans',
  display: 'swap',
})

const fraunces = Fraunces({
  subsets: ['latin'],
  variable: '--font-serif',
  display: 'swap',
  axes: ['opsz'],       // optical size axis (variable font)
})

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="es" className={`${jakarta.variable} ${fraunces.variable}`}>
      <body className="font-sans antialiased bg-[--bg] text-[--text]">
        {children}
      </body>
    </html>
  )
}

export const metadata = {
  title: 'Stampia — Diseña tu playera',
  description: 'Plataforma print-on-demand. Sube tu diseño, recíbelo en casa.',
}
```

- [ ] **Step 2: Reescribir globals.css con el nuevo design system**

```css
/* app/globals.css */
@import "tailwindcss";

@theme {
  --color-brand: #6c47ff;
  --color-brand-soft: #f0ecff;
  --color-bg: #fafaf9;
  --color-surface: #ffffff;
  --color-border: #ebebeb;
  --color-text: #0a0a0a;
  --color-muted: #6b7280;
  --color-faint: #9ca3af;

  --font-sans: var(--font-sans), system-ui, sans-serif;
  --font-serif: var(--font-serif), Georgia, serif;

  --radius-card: 16px;
  --radius-btn: 10px;
}

/* Base */
* { box-sizing: border-box; }

body {
  background-color: var(--color-bg);
  color: var(--color-text);
}

/* Typography utilities */
.font-serif { font-family: var(--font-serif); }
.font-sans  { font-family: var(--font-sans); }

/* Section spacing */
.section-py { padding-top: 120px; padding-bottom: 120px; }
@media (max-width: 768px) {
  .section-py { padding-top: 64px; padding-bottom: 64px; }
}

/* Container */
.container {
  max-width: 1200px;
  margin-left: auto;
  margin-right: auto;
  padding-left: 24px;
  padding-right: 24px;
}

.container-narrow {
  max-width: 960px;
  margin-left: auto;
  margin-right: auto;
  padding-left: 24px;
  padding-right: 24px;
}

/* ─── Animaciones ─── */

/* fade-up: reveal al entrar en viewport */
@keyframes fade-up {
  from {
    opacity: 0;
    transform: translateY(24px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* scale-reveal */
@keyframes scale-reveal {
  from {
    opacity: 0;
    transform: scale(0.96);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}

/* Clases de animación — activadas cuando el elemento tiene la clase 'is-visible' */
.animate-fade-up {
  opacity: 0;
  transform: translateY(24px);
  transition: none;
}

.animate-fade-up.is-visible {
  animation: fade-up 0.6s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  animation-delay: calc(var(--i, 0) * 0.1s);
}

.animate-scale-reveal {
  opacity: 0;
  transform: scale(0.96);
}

.animate-scale-reveal.is-visible {
  animation: scale-reveal 0.7s cubic-bezier(0.16, 1, 0.3, 1) forwards;
}

/* Botones */
.btn-primary {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background-color: var(--color-brand);
  color: #fff;
  font-family: var(--font-sans);
  font-weight: 700;
  font-size: 0.9375rem;
  padding: 12px 24px;
  border-radius: var(--radius-btn);
  border: none;
  cursor: pointer;
  transition: opacity 0.15s ease, transform 0.15s ease;
  text-decoration: none;
}

.btn-primary:hover {
  opacity: 0.9;
  transform: translateY(-1px);
}

.btn-secondary {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background-color: transparent;
  color: var(--color-text);
  font-family: var(--font-sans);
  font-weight: 600;
  font-size: 0.9375rem;
  padding: 11px 23px;
  border-radius: var(--radius-btn);
  border: 1.5px solid var(--color-border);
  cursor: pointer;
  transition: border-color 0.15s ease, transform 0.15s ease;
  text-decoration: none;
}

.btn-secondary:hover {
  border-color: var(--color-text);
  transform: translateY(-1px);
}

/* Card base */
.card {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-card);
}

/* Label uppercase */
.label-sm {
  font-family: var(--font-sans);
  font-size: 0.6875rem;
  font-weight: 700;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--color-faint);
}
```

- [ ] **Step 3: Verificar que Tailwind v4 reconoce las variables**

```bash
npm run dev
```

Abre `http://localhost:3000` — la página debe cargar sin errores de CSS.

- [ ] **Step 4: Commit**

```bash
git add app/layout.tsx app/globals.css
git commit -m "feat(ui): add Fraunces font, new design tokens and animation system in globals.css"
```

---

### Task 2: Componente AnimateOnScroll

**Files:**
- Create: `components/ui/AnimateOnScroll.tsx`

- [ ] **Step 1: Crear el componente**

```typescript
// components/ui/AnimateOnScroll.tsx
'use client'

import { useEffect, useRef } from 'react'

interface Props {
  children: React.ReactNode
  className?: string
  animation?: 'fade-up' | 'scale-reveal'
  delay?: number   // índice para stagger (0, 1, 2...)
  threshold?: number
}

export default function AnimateOnScroll({
  children,
  className = '',
  animation = 'fade-up',
  delay = 0,
  threshold = 0.15,
}: Props) {
  const ref = useRef<HTMLDivElement>(null)

  useEffect(() => {
    const el = ref.current
    if (!el) return

    el.style.setProperty('--i', String(delay))

    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) {
          el.classList.add('is-visible')
          observer.unobserve(el)
        }
      },
      { threshold }
    )

    observer.observe(el)
    return () => observer.disconnect()
  }, [delay, threshold])

  return (
    <div ref={ref} className={`animate-${animation} ${className}`}>
      {children}
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add components/ui/AnimateOnScroll.tsx
git commit -m "feat(ui): add AnimateOnScroll component with IntersectionObserver"
```

---

### Task 3: Rediseñar nav y footer — shop layout

**Files:**
- Modify: `app/(shop)/layout.tsx`

- [ ] **Step 1: Reescribir app/(shop)/layout.tsx**

```tsx
// app/(shop)/layout.tsx
import Link from 'next/link'
import { createClient } from '@/lib/supabase/server'
import CartBadge from '@/components/ui/CartBadge'
import NavUserMenu from '@/components/ui/NavUserMenu'

export default async function ShopLayout({ children }: { children: React.ReactNode }) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()

  return (
    <div className="min-h-screen flex flex-col" style={{ background: 'var(--color-bg)' }}>
      {/* Nav */}
      <header
        className="sticky top-0 z-50 border-b"
        style={{
          background: 'rgba(250, 250, 249, 0.85)',
          borderColor: 'var(--color-border)',
          backdropFilter: 'blur(12px)',
          WebkitBackdropFilter: 'blur(12px)',
        }}
      >
        <div className="container flex items-center justify-between h-16">
          <Link
            href="/"
            className="font-sans font-black text-sm tracking-widest uppercase"
            style={{ color: 'var(--color-text)' }}
          >
            Stampia
          </Link>

          <nav className="hidden md:flex items-center gap-8">
            <Link
              href="/catalog"
              className="font-sans text-sm font-medium transition-colors"
              style={{ color: 'var(--color-muted)' }}
            >
              Catálogo
            </Link>
          </nav>

          <div className="flex items-center gap-3">
            <CartBadge />
            {user ? (
              <NavUserMenu />
            ) : (
              <Link href="/login" className="btn-primary text-sm py-2 px-4">
                Entrar
              </Link>
            )}
          </div>
        </div>
      </header>

      {/* Main */}
      <main className="flex-1">{children}</main>

      {/* Footer */}
      <footer className="border-t section-py" style={{ borderColor: 'var(--color-border)' }}>
        <div className="container">
          <div className="flex flex-col md:flex-row justify-between gap-8">
            <div>
              <p className="font-sans font-black text-sm tracking-widest uppercase mb-2">
                Stampia
              </p>
              <p className="text-sm" style={{ color: 'var(--color-muted)' }}>
                Plataforma print-on-demand para México.
              </p>
            </div>
            <div className="flex gap-8">
              <div>
                <p className="label-sm mb-3">Tienda</p>
                <div className="flex flex-col gap-2">
                  <Link href="/catalog" className="text-sm" style={{ color: 'var(--color-muted)' }}>Catálogo</Link>
                  <Link href="/cart" className="text-sm" style={{ color: 'var(--color-muted)' }}>Carrito</Link>
                </div>
              </div>
              <div>
                <p className="label-sm mb-3">Cuenta</p>
                <div className="flex flex-col gap-2">
                  <Link href="/orders" className="text-sm" style={{ color: 'var(--color-muted)' }}>Mis órdenes</Link>
                  <Link href="/login" className="text-sm" style={{ color: 'var(--color-muted)' }}>Iniciar sesión</Link>
                </div>
              </div>
            </div>
          </div>
          <div
            className="mt-12 pt-8 border-t text-sm"
            style={{ borderColor: 'var(--color-border)', color: 'var(--color-faint)' }}
          >
            © {new Date().getFullYear()} Stampia. Todos los derechos reservados.
          </div>
        </div>
      </footer>
    </div>
  )
}
```

- [ ] **Step 2: Verificar en navegador**

```bash
npm run dev
```

Abre `http://localhost:3000` — el nav debe mostrase limpio con el logo STAMPIA en uppercase y los botones de carrito y login.

- [ ] **Step 3: Commit**

```bash
git add app/(shop)/layout.tsx
git commit -m "feat(ui): redesign shop layout — clean nav with blur, minimal footer"
```

---

### Task 4: Landing page — Hero y Stats

**Files:**
- Modify: `app/(shop)/page.tsx`

- [ ] **Step 1: Reescribir la sección Hero + Stats de la landing**

El hero tiene parallax. Crea el hook de parallax inline en el componente:

```tsx
// app/(shop)/page.tsx — fragmento del hero
'use client'

import { useEffect, useRef } from 'react'
import Link from 'next/link'
import MockupPlayera from '@/components/ui/MockupPlayera'
import AnimateOnScroll from '@/components/ui/AnimateOnScroll'

// Hook de parallax
function useParallax(factor = 0.15) {
  const ref = useRef<HTMLDivElement>(null)

  useEffect(() => {
    const el = ref.current
    if (!el) return

    const onScroll = () => {
      if (!el) return
      el.style.transform = `translateY(${window.scrollY * -factor}px)`
    }

    let raf: number
    const handler = () => { raf = requestAnimationFrame(onScroll) }
    window.addEventListener('scroll', handler, { passive: true })
    return () => {
      window.removeEventListener('scroll', handler)
      cancelAnimationFrame(raf)
    }
  }, [factor])

  return ref
}

// Hero
function Hero() {
  const parallaxRef = useParallax(0.12)

  return (
    <section
      className="section-py overflow-hidden"
      style={{ borderBottom: '1px solid var(--color-border)' }}
    >
      <div className="container">
        <div className="grid md:grid-cols-2 gap-16 items-center">
          {/* Texto */}
          <div>
            <AnimateOnScroll delay={0}>
              <p className="label-sm mb-4">Plataforma POD · México</p>
            </AnimateOnScroll>
            <AnimateOnScroll delay={1}>
              <h1
                className="font-serif italic text-6xl md:text-7xl leading-[1.05] tracking-tight mb-6"
                style={{ color: 'var(--color-text)' }}
              >
                Diseña tu<br />
                <span style={{ color: 'var(--color-brand)', fontStyle: 'normal' }}>
                  playera.
                </span>
              </h1>
            </AnimateOnScroll>
            <AnimateOnScroll delay={2}>
              <p
                className="text-lg leading-relaxed mb-8 max-w-md"
                style={{ color: 'var(--color-muted)' }}
              >
                Sube tu diseño, elige tu talla y recíbelo en casa.
                Sin mínimos, sin inventario, desde $299 MXN.
              </p>
            </AnimateOnScroll>
            <AnimateOnScroll delay={3}>
              <div className="flex items-center gap-4">
                <Link href="/catalog" className="btn-primary">
                  Empezar ahora
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5">
                    <path d="M5 12h14M12 5l7 7-7 7" />
                  </svg>
                </Link>
                <Link href="/catalog" className="btn-secondary">
                  Ver catálogo
                </Link>
              </div>
            </AnimateOnScroll>
          </div>

          {/* Playera con parallax */}
          <div className="flex justify-center">
            <div ref={parallaxRef} className="will-change-transform">
              <MockupPlayera color="#f0ece8" size={340} />
            </div>
          </div>
        </div>
      </div>
    </section>
  )
}

// Stats
function Stats() {
  const stats = [
    { value: '2,400+', label: 'Diseños entregados' },
    { value: '$299', label: 'Desde MXN' },
    { value: '5–7', label: 'Días de entrega' },
  ]

  return (
    <section
      className="py-16 border-b"
      style={{ borderColor: 'var(--color-border)' }}
    >
      <div className="container">
        <div className="grid grid-cols-3 gap-8">
          {stats.map((stat, i) => (
            <AnimateOnScroll key={stat.label} animation="scale-reveal" delay={i}>
              <div className="text-center">
                <p
                  className="font-serif italic text-4xl md:text-5xl font-bold mb-2"
                  style={{ color: 'var(--color-text)' }}
                >
                  {stat.value}
                </p>
                <p className="label-sm">{stat.label}</p>
              </div>
            </AnimateOnScroll>
          ))}
        </div>
      </div>
    </section>
  )
}
```

- [ ] **Step 2: Verificar el parallax en navegador**

```bash
npm run dev
```

Navega a `http://localhost:3000`. La playera debe moverse ligeramente al hacer scroll. Las animaciones de fade-up deben dispararse al entrar en viewport.

- [ ] **Step 3: Commit (parcial — solo Hero + Stats)**

```bash
git add app/(shop)/page.tsx
git commit -m "feat(ui): landing hero with parallax product, stats bar with scale-reveal"
```

---

### Task 5: Landing page — Cómo funciona, Showcase, Beneficios, Testimonios, Comparativa, CTA

**Files:**
- Modify: `app/(shop)/page.tsx`

- [ ] **Step 1: Agregar la sección Cómo Funciona**

```tsx
function HowItWorks() {
  const steps = [
    {
      n: '01',
      title: 'Elige tu producto',
      desc: 'Selecciona entre nuestra colección de playeras premium en diferentes colores y tallas.',
    },
    {
      n: '02',
      title: 'Sube tu diseño',
      desc: 'Arrastra tu archivo PNG o JPG. Posiciona, escala y ajusta en el editor visual.',
    },
    {
      n: '03',
      title: 'Recíbelo en casa',
      desc: 'Procesamos tu orden y la enviamos a toda la República Mexicana en 5–7 días hábiles.',
    },
  ]

  return (
    <section className="section-py border-b" style={{ borderColor: 'var(--color-border)' }}>
      <div className="container-narrow">
        <AnimateOnScroll>
          <p className="label-sm mb-4">Proceso</p>
          <h2
            className="font-serif italic text-4xl md:text-5xl tracking-tight mb-16"
            style={{ color: 'var(--color-text)' }}
          >
            Tres pasos.<br />
            <span style={{ color: 'var(--color-brand)', fontStyle: 'normal' }}>
              Tu diseño impreso.
            </span>
          </h2>
        </AnimateOnScroll>

        <div className="grid md:grid-cols-3 gap-12">
          {steps.map((step, i) => (
            <AnimateOnScroll key={step.n} delay={i}>
              <div>
                <p
                  className="font-serif italic text-8xl font-bold mb-4 leading-none select-none"
                  style={{ color: 'var(--color-brand-soft)', WebkitTextStroke: `1px var(--color-brand)` }}
                >
                  {step.n}
                </p>
                <h3 className="font-sans font-bold text-xl mb-2">{step.title}</h3>
                <p className="text-base leading-relaxed" style={{ color: 'var(--color-muted)' }}>
                  {step.desc}
                </p>
              </div>
            </AnimateOnScroll>
          ))}
        </div>
      </div>
    </section>
  )
}
```

- [ ] **Step 2: Agregar Showcase de productos**

```tsx
// Este componente es server — se pasa desde el Page como prop
// En page.tsx (server component wrapper), pasa los productos al componente client

// ProductWithVariants viene de @/types/product
import type { ProductWithVariants } from '@/types/product'

function ProductShowcase({ products }: { products: ProductWithVariants[] }) {
  return (
    <section className="section-py border-b" style={{ borderColor: 'var(--color-border)' }}>
      <div className="container">
        <AnimateOnScroll>
          <p className="label-sm mb-4">Catálogo</p>
          <h2
            className="font-serif italic text-4xl md:text-5xl tracking-tight mb-16"
            style={{ color: 'var(--color-text)' }}
          >
            Productos disponibles.
          </h2>
        </AnimateOnScroll>
        <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
          {products.map((product, i) => (
            <AnimateOnScroll key={product.id} delay={i % 3}>
              <ProductCard product={product} />
            </AnimateOnScroll>
          ))}
        </div>
        <AnimateOnScroll>
          <div className="mt-12 text-center">
            <Link href="/catalog" className="btn-secondary">
              Ver todo el catálogo
            </Link>
          </div>
        </AnimateOnScroll>
      </div>
    </section>
  )
}
```

- [ ] **Step 3: Agregar Beneficios**

```tsx
function Benefits() {
  const items = [
    'Sin pedido mínimo — imprime desde 1 pieza',
    'Entrega a toda la República Mexicana',
    'Impresión DTG de alta calidad, colores vibrantes',
    'Pagos seguros con MercadoPago (tarjeta, OXXO, SPEI)',
    'Soporte por WhatsApp si algo sale mal',
  ]

  return (
    <section
      className="section-py border-b"
      style={{
        background: 'var(--color-brand-soft)',
        borderColor: 'var(--color-border)',
      }}
    >
      <div className="container">
        <div className="grid md:grid-cols-2 gap-16 items-start">
          <AnimateOnScroll>
            <p className="label-sm mb-4">¿Por qué Stampia?</p>
            <h2
              className="font-serif italic text-4xl md:text-5xl tracking-tight"
              style={{ color: 'var(--color-text)' }}
            >
              La forma más<br />fácil de imprimir<br />
              <span style={{ color: 'var(--color-brand)', fontStyle: 'normal' }}>
                en México.
              </span>
            </h2>
          </AnimateOnScroll>

          <div className="flex flex-col gap-4 pt-2">
            {items.map((item, i) => (
              <AnimateOnScroll key={item} delay={i}>
                <div
                  className="flex items-start gap-4 py-4 border-b"
                  style={{ borderColor: 'rgba(108,71,255,0.15)' }}
                >
                  <div
                    className="w-0.5 self-stretch rounded-full flex-shrink-0"
                    style={{ background: 'var(--color-brand)' }}
                  />
                  <p className="text-base leading-relaxed" style={{ color: 'var(--color-text)' }}>
                    {item}
                  </p>
                </div>
              </AnimateOnScroll>
            ))}
          </div>
        </div>
      </div>
    </section>
  )
}
```

- [ ] **Step 4: Agregar Testimonios**

```tsx
function Testimonials() {
  const quotes = [
    {
      text: '"Subí mi diseño y en una semana llegó la playera. La calidad superó mis expectativas."',
      name: 'Diego R.',
      city: 'CDMX',
    },
    {
      text: '"Por fin una plataforma que no te pide pedir 50 piezas. Ideal para artistas independientes."',
      name: 'Valeria M.',
      city: 'Guadalajara',
    },
    {
      text: '"El proceso es súper intuitivo. Tardé menos de 10 minutos desde el diseño hasta el pago."',
      name: 'Carlos T.',
      city: 'Monterrey',
    },
  ]

  return (
    <section className="section-py border-b" style={{ borderColor: 'var(--color-border)' }}>
      <div className="container-narrow">
        <AnimateOnScroll>
          <p className="label-sm mb-4">Testimonios</p>
          <h2
            className="font-serif italic text-4xl md:text-5xl tracking-tight mb-16"
            style={{ color: 'var(--color-text)' }}
          >
            Lo que dicen<br />nuestros clientes.
          </h2>
        </AnimateOnScroll>

        <div className="grid md:grid-cols-3 gap-8">
          {quotes.map((q, i) => (
            <AnimateOnScroll key={q.name} delay={i}>
              <div className="card p-8">
                <p
                  className="font-serif italic text-xl leading-relaxed mb-6"
                  style={{ color: 'var(--color-text)' }}
                >
                  {q.text}
                </p>
                <div>
                  <p className="font-sans font-semibold text-sm">{q.name}</p>
                  <p className="label-sm mt-0.5">{q.city}</p>
                </div>
              </div>
            </AnimateOnScroll>
          ))}
        </div>
      </div>
    </section>
  )
}
```

- [ ] **Step 5: Agregar Comparativa y CTA**

```tsx
function Comparison() {
  const features = [
    { label: 'Sin pedido mínimo', stampia: true, others: false },
    { label: 'Entrega a México completo', stampia: true, others: true },
    { label: 'Editor visual en línea', stampia: true, others: false },
    { label: 'Pagos con OXXO y SPEI', stampia: true, others: false },
    { label: 'Preview en tiempo real', stampia: true, others: false },
  ]

  return (
    <section className="section-py border-b" style={{ borderColor: 'var(--color-border)' }}>
      <div className="container-narrow">
        <AnimateOnScroll>
          <p className="label-sm mb-4">Comparativa</p>
          <h2
            className="font-serif italic text-4xl tracking-tight mb-12"
            style={{ color: 'var(--color-text)' }}
          >
            Stampia vs. el resto.
          </h2>
        </AnimateOnScroll>

        {/* No wrappear <tr> en <div> — AnimateOnScroll lo haría. Usar fade-in CSS directo. */}
        <table className="w-full">
          <thead>
            <tr className="border-b" style={{ borderColor: 'var(--color-border)' }}>
              <th className="text-left pb-4 label-sm w-1/2">Característica</th>
              <th
                className="pb-4 label-sm text-center"
                style={{ color: 'var(--color-brand)', background: 'var(--color-brand-soft)', borderRadius: '8px 8px 0 0', padding: '8px 16px' }}
              >
                Stampia
              </th>
              <th className="pb-4 label-sm text-center">Otros</th>
            </tr>
          </thead>
          <tbody>
            {features.map((f, i) => (
              <tr
                key={f.label}
                className="border-b"
                style={{
                  borderColor: 'var(--color-border)',
                  opacity: 0,
                  animation: `fade-up 0.5s cubic-bezier(0.16,1,0.3,1) ${i * 0.08}s forwards`,
                }}
              >
                <td className="py-4 text-sm">{f.label}</td>
                <td
                  className="py-4 text-center text-sm font-bold"
                  style={{ background: 'var(--color-brand-soft)', color: f.stampia ? 'var(--color-brand)' : 'var(--color-faint)' }}
                >
                  {f.stampia ? '✓' : '—'}
                </td>
                <td
                  className="py-4 text-center text-sm"
                  style={{ color: f.others ? 'var(--color-text)' : 'var(--color-faint)' }}
                >
                  {f.others ? '✓' : '—'}
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </section>
  )
}

function CTA() {
  return (
    <section className="section-py">
      <div className="container-narrow text-center">
        <AnimateOnScroll>
          <h2
            className="font-serif italic text-5xl md:text-6xl tracking-tight mb-6"
            style={{ color: 'var(--color-text)' }}
          >
            Tu diseño, impreso.<br />
            <span style={{ color: 'var(--color-brand)', fontStyle: 'normal' }}>
              Sin complicaciones.
            </span>
          </h2>
        </AnimateOnScroll>
        <AnimateOnScroll delay={1}>
          <p
            className="text-lg mb-10 max-w-md mx-auto"
            style={{ color: 'var(--color-muted)' }}
          >
            Únete a miles de creadores que ya usan Stampia para imprimir sus ideas.
          </p>
          <Link href="/catalog" className="btn-primary text-lg py-4 px-8">
            Empezar gratis
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5">
              <path d="M5 12h14M12 5l7 7-7 7" />
            </svg>
          </Link>
        </AnimateOnScroll>
      </div>
    </section>
  )
}
```

- [ ] **Step 6: Ensamblar todo en el Page export**

```tsx
// Al final de page.tsx — el export default
export default async function HomePage() {
  const supabase = await createClient()
  const { data: products } = await supabase
    .from('products')
    .select('id, name, slug, base_price_mxn, mockup_front_url, product_variants(id, color_name, color_hex, size)')
    .eq('active', true)
    .limit(6)

  return (
    <>
      <Hero />
      <Stats />
      <HowItWorks />
      <ProductShowcase products={products ?? []} />
      <Benefits />
      <Testimonials />
      <Comparison />
      <CTA />
    </>
  )
}

export const revalidate = 60
```

- [ ] **Step 7: Verificar landing completa en navegador**

```bash
npm run dev
```

Navega a `http://localhost:3000`. Verifica:
- El parallax de la playera funciona al scroll
- Las secciones aparecen con animación al entrar en viewport
- La comparativa y CTA se ven bien en mobile (resize a 375px)

- [ ] **Step 8: Commit**

```bash
git add app/(shop)/page.tsx
git commit -m "feat(ui): complete landing page redesign — all 8 sections with editorial layout and animations"
```

---

### Task 6: Rediseñar ProductCard

**Files:**
- Modify: `components/product/ProductCard.tsx`

- [ ] **Step 1: Reescribir ProductCard.tsx**

```tsx
// components/product/ProductCard.tsx
import Link from 'next/link'
import MockupPlayera from '@/components/ui/MockupPlayera'
import type { ProductWithVariants } from '@/types/product'

interface Props {
  product: ProductWithVariants
}

export default function ProductCard({ product }: Props) {
  const uniqueColors = [...new Map(
    product.product_variants.map(v => [v.color_hex, v])
  ).values()]

  const firstColor = uniqueColors[0]

  return (
    <Link
      href={`/product/${product.slug}`}
      className="group block card overflow-hidden hover:shadow-lg transition-shadow duration-300"
    >
      {/* Mockup */}
      <div
        className="relative flex items-center justify-center p-8"
        style={{ background: 'var(--color-bg)', minHeight: '260px' }}
      >
        <div className="transition-transform duration-500 group-hover:scale-105 will-change-transform">
          <MockupPlayera color={firstColor?.color_hex ?? '#f0ece8'} size={200} />
        </div>
      </div>

      {/* Info */}
      <div className="p-6 border-t" style={{ borderColor: 'var(--color-border)' }}>
        <div className="flex items-start justify-between mb-3">
          <h3 className="font-sans font-bold text-base">{product.name}</h3>
          <p
            className="font-serif italic text-lg font-bold flex-shrink-0 ml-2"
            style={{ color: 'var(--color-brand)' }}
          >
            ${Number(product.base_price_mxn).toFixed(0)}
          </p>
        </div>

        {/* Color swatches */}
        <div className="flex items-center gap-1.5">
          {uniqueColors.slice(0, 5).map(v => (
            <div
              key={v.color_hex}
              className="w-4 h-4 rounded-full border-2 transition-transform hover:scale-110"
              style={{
                background: v.color_hex,
                borderColor: 'var(--color-border)',
              }}
              title={v.color_name}
            />
          ))}
          {uniqueColors.length > 5 && (
            <span className="text-xs" style={{ color: 'var(--color-faint)' }}>
              +{uniqueColors.length - 5}
            </span>
          )}
        </div>
      </div>
    </Link>
  )
}
```

- [ ] **Step 2: Verificar en /catalog**

```bash
npm run dev
```

Navega a `/catalog`. Las cards deben mostrar el hover sutil sin sombra agresiva, y el precio en Fraunces italic.

- [ ] **Step 3: Commit**

```bash
git add components/product/ProductCard.tsx
git commit -m "feat(ui): redesign ProductCard — clean border card, Fraunces price, subtle hover"
```

---

### Task 7: Rediseñar catálogo y detalle de producto

**Files:**
- Modify: `app/(shop)/catalog/page.tsx`
- Modify: `app/(shop)/product/[slug]/page.tsx`
- Modify: `components/product/ProductDetail.tsx`

- [ ] **Step 1: Actualizar catalog/page.tsx**

```tsx
// app/(shop)/catalog/page.tsx (server component)
import { createClient } from '@/lib/supabase/server'
import ProductCard from '@/components/product/ProductCard'
import AnimateOnScroll from '@/components/ui/AnimateOnScroll'

export const revalidate = 60

export default async function CatalogPage() {
  const supabase = await createClient()
  const { data: products } = await supabase
    .from('products')
    .select('id, name, slug, base_price_mxn, mockup_front_url, product_variants(id, color_name, color_hex, size, extra_price_mxn)')
    .eq('active', true)
    .order('created_at', { ascending: false })

  return (
    <div className="section-py">
      <div className="container">
        <AnimateOnScroll>
          <div className="mb-12">
            <p className="label-sm mb-3">Catálogo</p>
            <h1
              className="font-serif italic text-5xl tracking-tight"
              style={{ color: 'var(--color-text)' }}
            >
              Todos los productos.
            </h1>
            <p className="mt-3 text-sm" style={{ color: 'var(--color-muted)' }}>
              {(products ?? []).length} producto{(products ?? []).length !== 1 ? 's' : ''} disponible{(products ?? []).length !== 1 ? 's' : ''}
            </p>
          </div>
        </AnimateOnScroll>

        <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
          {(products ?? []).map((product, i) => (
            <AnimateOnScroll key={product.id} delay={i % 3}>
              <ProductCard product={product as any} />
            </AnimateOnScroll>
          ))}
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Actualizar product/[slug]/page.tsx**

El layout del detalle cambia a split: mockup sticky izquierda, selectors derecha.

```tsx
// app/(shop)/product/[slug]/page.tsx — pasar product a ProductDetail sin cambios en la lógica de fetch
// Solo agregar el wrapper de layout si no existe:

export default async function ProductPage({ params }: Props) {
  const { slug } = await params
  // ... fetch igual que antes ...

  return (
    <div className="section-py">
      <div className="container">
        <ProductDetail product={product} />
      </div>
    </div>
  )
}
```

- [ ] **Step 3: Actualizar ProductDetail.tsx con el nuevo layout split**

```tsx
// components/product/ProductDetail.tsx — fragmento clave del layout
// Mantener toda la lógica de state (color/talla selector, navigate to /customize)
// Solo cambiar el JSX exterior:

return (
  <div className="grid md:grid-cols-2 gap-16 items-start">
    {/* Mockup sticky */}
    <div className="md:sticky md:top-24">
      <div
        className="rounded-2xl flex items-center justify-center p-12"
        style={{ background: 'var(--color-bg)', border: '1px solid var(--color-border)' }}
      >
        <div className="animate-scale-reveal is-visible">
          <MockupPlayera color={selectedColor?.color_hex ?? '#f0ece8'} size={280} />
        </div>
      </div>
    </div>

    {/* Selectors */}
    <div>
      <p className="label-sm mb-3">{product.name}</p>
      <p
        className="font-serif italic text-4xl font-bold mb-6"
        style={{ color: 'var(--color-text)' }}
      >
        ${basePrice.toFixed(0)}
        <span className="text-base font-sans font-normal ml-1" style={{ color: 'var(--color-muted)' }}>MXN</span>
      </p>

      {/* Color selector */}
      <div className="mb-6">
        <p className="label-sm mb-3">Color — {selectedColor?.color_name}</p>
        <div className="flex gap-2 flex-wrap">
          {uniqueColors.map(color => (
            <button
              key={color.color_hex}
              onClick={() => setSelectedColor(color)}
              className="w-8 h-8 rounded-full transition-transform hover:scale-110"
              style={{
                background: color.color_hex,
                outline: selectedColor?.color_hex === color.color_hex
                  ? `2px solid var(--color-brand)`
                  : `2px solid transparent`,
                outlineOffset: '2px',
                border: '1px solid var(--color-border)',
              }}
              title={color.color_name}
            />
          ))}
        </div>
      </div>

      {/* Talla selector */}
      <div className="mb-8">
        <p className="label-sm mb-3">Talla</p>
        <div className="flex gap-2 flex-wrap">
          {availableSizes.map(size => (
            <button
              key={size}
              onClick={() => setSelectedSize(size)}
              className="px-4 py-2 text-sm font-medium rounded-lg border transition-colors"
              style={{
                borderColor: selectedSize === size ? 'var(--color-brand)' : 'var(--color-border)',
                background: selectedSize === size ? 'var(--color-brand-soft)' : 'transparent',
                color: selectedSize === size ? 'var(--color-brand)' : 'var(--color-text)',
              }}
            >
              {size}
            </button>
          ))}
        </div>
      </div>

      <button
        onClick={handlePersonalize}
        disabled={!selectedSize}
        className="btn-primary w-full justify-center py-4 text-base"
      >
        Personalizar este producto
      </button>

      <p className="mt-4 text-sm text-center" style={{ color: 'var(--color-muted)' }}>
        Envío a toda la República Mexicana · 5–7 días hábiles
      </p>
    </div>
  </div>
)
```

- [ ] **Step 4: Verificar en navegador**

Navega a `/catalog` → click en un producto → debe mostrar el layout split con el mockup pegajoso en desktop.

- [ ] **Step 5: Commit**

```bash
git add app/(shop)/catalog/page.tsx app/(shop)/product/[slug]/page.tsx components/product/ProductDetail.tsx
git commit -m "feat(ui): redesign catalog and product detail — split layout with sticky mockup"
```

---

### Task 8: Rediseñar carrito y checkout

**Files:**
- Modify: `app/(shop)/cart/page.tsx`
- Modify: `app/(shop)/checkout/page.tsx`

- [ ] **Step 1: Actualizar cart/page.tsx**

Reemplaza el fondo gris y las sombras por el estilo limpio con bordes:

```tsx
// app/(shop)/cart/page.tsx — estructura clave
// Mantener toda la lógica de Zustand, solo cambiar el JSX:

return (
  <div className="section-py">
    <div className="container-narrow">
      <h1
        className="font-serif italic text-4xl tracking-tight mb-12"
        style={{ color: 'var(--color-text)' }}
      >
        Tu carrito.
      </h1>

      {items.length === 0 ? (
        <div className="text-center py-24">
          <p className="font-serif italic text-2xl mb-6" style={{ color: 'var(--color-muted)' }}>
            Tu carrito está vacío.
          </p>
          <Link href="/catalog" className="btn-primary">Ver catálogo</Link>
        </div>
      ) : (
        <div className="grid md:grid-cols-3 gap-12">
          {/* Items */}
          <div className="md:col-span-2">
            {items.map((item, i) => (
              <div
                key={`${item.variantId}-${item.designUrl}`}
                className="flex gap-4 py-6 border-b"
                style={{ borderColor: 'var(--color-border)' }}
              >
                <div
                  className="w-20 h-20 rounded-xl flex-shrink-0 flex items-center justify-center"
                  style={{ background: 'var(--color-bg)', border: '1px solid var(--color-border)' }}
                >
                  <MockupPlayera color={item.colorHex} size={60} />
                </div>
                <div className="flex-1">
                  <p className="font-sans font-semibold text-sm mb-0.5">{item.productName}</p>
                  <p className="text-xs mb-3" style={{ color: 'var(--color-muted)' }}>
                    {item.colorName} · {item.size}
                  </p>
                  {/* Quantity controls + remove — sin cambios en la lógica */}
                </div>
                <div className="text-right">
                  <p className="font-serif italic font-bold">
                    ${(item.unitPriceMxn * item.quantity).toFixed(0)}
                  </p>
                </div>
              </div>
            ))}
          </div>

          {/* Order summary */}
          <div>
            <div
              className="card p-6 sticky top-24"
            >
              <h3 className="font-sans font-bold mb-4">Resumen</h3>
              <div className="flex justify-between text-sm mb-2">
                <span style={{ color: 'var(--color-muted)' }}>Subtotal</span>
                <span>${subtotal.toFixed(0)} MXN</span>
              </div>
              <div className="flex justify-between text-sm mb-4">
                <span style={{ color: 'var(--color-muted)' }}>Envío</span>
                <span>$99 MXN</span>
              </div>
              <div
                className="flex justify-between font-bold text-base pt-4 border-t mb-6"
                style={{ borderColor: 'var(--color-border)' }}
              >
                <span>Total</span>
                <span className="font-serif italic">${(subtotal + 99).toFixed(0)} MXN</span>
              </div>
              <Link href="/checkout" className="btn-primary w-full justify-center">
                Proceder al pago
              </Link>
            </div>
          </div>
        </div>
      )}
    </div>
  </div>
)
```

- [ ] **Step 2: Actualizar checkout/page.tsx — form minimalista**

```tsx
// app/(shop)/checkout/page.tsx — estructura del form
// Mantener toda la lógica de submit y createOrder, solo cambiar el JSX:

return (
  <div className="section-py">
    <div className="container-narrow">
      <h1
        className="font-serif italic text-4xl tracking-tight mb-12"
        style={{ color: 'var(--color-text)' }}
      >
        Datos de envío.
      </h1>

      <div className="grid md:grid-cols-2 gap-12">
        <form onSubmit={handleSubmit} className="flex flex-col gap-5">
          {/* Campos sin floating label, diseño limpio */}
          {[
            { name: 'name', label: 'Nombre completo', type: 'text' },
            { name: 'phone', label: 'Teléfono', type: 'tel' },
            { name: 'street', label: 'Calle y número', type: 'text' },
            { name: 'city', label: 'Ciudad', type: 'text' },
            { name: 'zip', label: 'Código postal', type: 'text' },
          ].map(field => (
            <div key={field.name}>
              <label className="label-sm block mb-1.5">{field.label}</label>
              <input
                name={field.name}
                type={field.type}
                required
                className="w-full px-4 py-3 rounded-xl text-sm outline-none transition-colors"
                style={{
                  background: 'var(--color-surface)',
                  border: '1.5px solid var(--color-border)',
                  color: 'var(--color-text)',
                }}
                onFocus={e => (e.target.style.borderColor = 'var(--color-brand)')}
                onBlur={e => (e.target.style.borderColor = 'var(--color-border)')}
              />
            </div>
          ))}

          {/* Estado */}
          <div>
            <label className="label-sm block mb-1.5">Estado</label>
            <select
              name="state"
              required
              className="w-full px-4 py-3 rounded-xl text-sm outline-none"
              style={{
                background: 'var(--color-surface)',
                border: '1.5px solid var(--color-border)',
                color: 'var(--color-text)',
              }}
            >
              <option value="">Selecciona un estado</option>
              {/* Los 32 estados — sin cambios */}
            </select>
          </div>

          {error && (
            <p className="text-sm" style={{ color: '#ef4444' }}>{error}</p>
          )}

          <button type="submit" disabled={loading} className="btn-primary w-full justify-center py-4 mt-2">
            {loading ? 'Procesando...' : 'Ir a pagar con MercadoPago'}
          </button>
        </form>

        {/* Order summary */}
        <div>
          <h3 className="font-sans font-bold mb-6">Tu orden</h3>
          <div className="flex flex-col gap-4">
            {items.map(item => (
              <div key={`${item.variantId}-${item.designUrl}`} className="flex items-center gap-3">
                <div
                  className="w-12 h-12 rounded-lg flex-shrink-0 flex items-center justify-center"
                  style={{ background: 'var(--color-bg)', border: '1px solid var(--color-border)' }}
                >
                  <MockupPlayera color={item.colorHex} size={40} />
                </div>
                <div className="flex-1 min-w-0">
                  <p className="text-sm font-medium truncate">{item.productName}</p>
                  <p className="text-xs" style={{ color: 'var(--color-muted)' }}>
                    {item.colorName} · {item.size} · ×{item.quantity}
                  </p>
                </div>
                <p className="text-sm font-semibold flex-shrink-0">
                  ${(item.unitPriceMxn * item.quantity).toFixed(0)}
                </p>
              </div>
            ))}
          </div>
          <div className="mt-4 pt-4 border-t" style={{ borderColor: 'var(--color-border)' }}>
            <div className="flex justify-between text-sm mb-1">
              <span style={{ color: 'var(--color-muted)' }}>Envío</span>
              <span>$99 MXN</span>
            </div>
            <div className="flex justify-between font-bold mt-2">
              <span>Total</span>
              <span className="font-serif italic">
                ${(items.reduce((s, i) => s + i.unitPriceMxn * i.quantity, 0) + 99).toFixed(0)} MXN
              </span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
)
```

- [ ] **Step 3: Verificar en navegador**

Navega a `/cart` y `/checkout`. Verifica responsividad en mobile.

- [ ] **Step 4: Commit**

```bash
git add app/(shop)/cart/page.tsx app/(shop)/checkout/page.tsx
git commit -m "feat(ui): redesign cart and checkout — clean border style, minimal form"
```

---

### Task 9: Rediseñar páginas de auth y cuenta

**Files:**
- Modify: `app/(auth)/layout.tsx`
- Modify: `app/(auth)/login/page.tsx`
- Modify: `app/(auth)/register/page.tsx`
- Modify: `app/(account)/orders/page.tsx`
- Modify: `app/(account)/orders/[orderId]/page.tsx`
- Modify: `components/ui/StatusBadge.tsx`

- [ ] **Step 1: Actualizar auth/layout.tsx**

```tsx
// app/(auth)/layout.tsx
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return (
    <div
      className="min-h-screen flex items-center justify-center p-4"
      style={{ background: 'var(--color-bg)' }}
    >
      <div className="w-full max-w-sm">
        <div className="text-center mb-8">
          <p className="font-sans font-black text-sm tracking-widest uppercase mb-1">Stampia</p>
        </div>
        {children}
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Actualizar login/page.tsx**

```tsx
// Mantener lógica de useActionState + login SA, solo cambiar el JSX:
return (
  <div>
    <h1
      className="font-serif italic text-3xl tracking-tight mb-8 text-center"
      style={{ color: 'var(--color-text)' }}
    >
      Bienvenido de vuelta.
    </h1>

    <form action={formAction} className="flex flex-col gap-4">
      <div>
        <label className="label-sm block mb-1.5">Correo</label>
        <input name="email" type="email" required className="w-full px-4 py-3 rounded-xl text-sm outline-none"
          style={{ background: 'var(--color-surface)', border: '1.5px solid var(--color-border)', color: 'var(--color-text)' }} />
      </div>
      <div>
        <label className="label-sm block mb-1.5">Contraseña</label>
        <input name="password" type="password" required className="w-full px-4 py-3 rounded-xl text-sm outline-none"
          style={{ background: 'var(--color-surface)', border: '1.5px solid var(--color-border)', color: 'var(--color-text)' }} />
      </div>

      {state?.error && <p className="text-sm" style={{ color: '#ef4444' }}>{state.error}</p>}

      <SubmitButton className="btn-primary w-full justify-center py-3 mt-2">
        Iniciar sesión
      </SubmitButton>
    </form>

    <p className="mt-6 text-center text-sm" style={{ color: 'var(--color-muted)' }}>
      ¿No tienes cuenta?{' '}
      <Link href="/register" style={{ color: 'var(--color-brand)' }}>Regístrate</Link>
    </p>
  </div>
)
```

- [ ] **Step 3: Actualizar register/page.tsx**

```tsx
// Mantener lógica de useActionState + register SA, solo cambiar el JSX:
return (
  <div>
    <h1
      className="font-serif italic text-3xl tracking-tight mb-8 text-center"
      style={{ color: 'var(--color-text)' }}
    >
      Crea tu cuenta.
    </h1>

    <form action={formAction} className="flex flex-col gap-4">
      <div>
        <label className="label-sm block mb-1.5">Nombre completo</label>
        <input name="full_name" type="text" required className="w-full px-4 py-3 rounded-xl text-sm outline-none"
          style={{ background: 'var(--color-surface)', border: '1.5px solid var(--color-border)', color: 'var(--color-text)' }} />
      </div>
      <div>
        <label className="label-sm block mb-1.5">Correo</label>
        <input name="email" type="email" required className="w-full px-4 py-3 rounded-xl text-sm outline-none"
          style={{ background: 'var(--color-surface)', border: '1.5px solid var(--color-border)', color: 'var(--color-text)' }} />
      </div>
      <div>
        <label className="label-sm block mb-1.5">Contraseña</label>
        <input name="password" type="password" required minLength={8} className="w-full px-4 py-3 rounded-xl text-sm outline-none"
          style={{ background: 'var(--color-surface)', border: '1.5px solid var(--color-border)', color: 'var(--color-text)' }} />
      </div>

      {state?.error && <p className="text-sm" style={{ color: '#ef4444' }}>{state.error}</p>}

      <SubmitButton className="btn-primary w-full justify-center py-3 mt-2">
        Crear cuenta
      </SubmitButton>
    </form>

    <p className="mt-6 text-center text-sm" style={{ color: 'var(--color-muted)' }}>
      ¿Ya tienes cuenta?{' '}
      <Link href="/login" style={{ color: 'var(--color-brand)' }}>Inicia sesión</Link>
    </p>
  </div>
)
```

- [ ] **Step 4: Actualizar StatusBadge.tsx**

```tsx
// components/ui/StatusBadge.tsx
const STATUS_CONFIG = {
  pending:    { label: 'Pendiente',   color: '#f59e0b' },
  paid:       { label: 'Pagado',      color: '#6c47ff' },
  processing: { label: 'En proceso',  color: '#3b82f6' },
  shipped:    { label: 'Enviado',     color: '#10b981' },
  delivered:  { label: 'Entregado',   color: '#059669' },
  cancelled:  { label: 'Cancelado',   color: '#ef4444' },
} as const

type Status = keyof typeof STATUS_CONFIG

export default function StatusBadge({ status }: { status: Status }) {
  const { label, color } = STATUS_CONFIG[status] ?? STATUS_CONFIG.pending

  return (
    <span
      className="inline-flex items-center gap-1.5 text-xs font-semibold px-3 py-1 rounded-full"
      style={{ background: `${color}15`, color }}
    >
      <span
        className="w-1.5 h-1.5 rounded-full flex-shrink-0"
        style={{ background: color }}
      />
      {label}
    </span>
  )
}
```

- [ ] **Step 5: Actualizar orders/page.tsx y orders/[orderId]/page.tsx**

Aplicar el mismo estilo limpio con bordes. Usar `StatusBadge` actualizado. Lógica sin cambios.

- [ ] **Step 6: Commit**

```bash
git add app/(auth)/layout.tsx app/(auth)/login/page.tsx app/(auth)/register/page.tsx \
        app/(account)/orders/page.tsx app/(account)/orders/[orderId]/page.tsx \
        components/ui/StatusBadge.tsx
git commit -m "feat(ui): redesign auth pages, orders, and StatusBadge with new design system"
```

---

### Task 10: Admin — cleanup tipográfico

**Files:**
- Modify: `app/(admin)/layout.tsx`

- [ ] **Step 1: Actualizar colores del sidebar admin**

El admin no necesita la expresividad de la tienda. Solo aplica los tokens de color consistentes:

```tsx
// app/(admin)/layout.tsx — fragmento del sidebar
// Reemplazar colores hardcodeados por variables CSS:
// bg-neutral-900 → style={{ background: 'var(--color-text)' }}
// text-white → color: '#fff'
// border-neutral-800 → borderColor: 'rgba(255,255,255,0.1)'
// hover states → rgba whites

// El contenido del main:
// bg-neutral-50 → style={{ background: 'var(--color-bg)' }}
```

- [ ] **Step 2: Build final**

```bash
npm run build
```

Resultado esperado: build exitoso. Si hay errores de TypeScript, corrígelos antes de continuar.

- [ ] **Step 3: Commit final**

```bash
git add app/(admin)/layout.tsx
git commit -m "feat(ui): apply design tokens to admin layout sidebar"
git commit -m "chore: Fase 2 UI redesign complete"
```

---

### Task 11: Smoke test visual completo

**Files:** Ninguno

- [ ] **Step 1: Verificar todas las páginas clave**

```bash
npm run dev
```

Checklist en `http://localhost:3000`:

| Página | Verificar |
|--------|-----------|
| `/` | Parallax hero, todas las secciones, animaciones al scroll |
| `/catalog` | Grid de productos, Fraunces en heading |
| `/product/[slug]` | Split layout sticky, swatch selector, talla selector |
| `/customize/[variantId]` | Canvas funcional, toolbar limpio |
| `/cart` | Items con mockup, summary sticky |
| `/checkout` | Form limpio, estados selector |
| `/login` | Card centrada, Fraunces italic heading |
| `/orders` | StatusBadge con dot de color |
| `/dashboard` | Admin sin errores |

- [ ] **Step 2: Verificar mobile (375px)**

En DevTools, simula iPhone SE (375px). Verificar:
- Hero: columna única
- Catalogo: 1 columna
- Carrito: summary abajo
- Checkout: columna única

- [ ] **Step 3: Commit final de verificación**

```bash
git commit --allow-empty -m "chore: smoke test passed — Fase 2 UI redesign complete"
```
